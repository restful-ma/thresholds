openapi: 3.0.0
info:
  title: DataFactoryManagementClient
  version: 2018-06-01
  x-apisguru-categories:
    - cloud
  x-logo:
    url: https://assets.onestore.ms/cdnfiles/onestorerolling-1606-01000/shell/v3/images/logo/microsoft.png
  x-origin:
    - format: swagger
      url: https://raw.githubusercontent.com/Azure/azure-rest-api-specs/master/specification/datafactory/resource-manager/Microsoft.DataFactory/stable/2018-06-01/entityTypes/Dataset.json
      version: "2.0"
  x-preferred: true
  x-providerName: azure.com
  x-serviceName: datafactory-Dataset
  x-tags:
    - Azure
    - Microsoft
paths: {}
servers:
  - url: https://azure.local
components:
  schemas:
    AmazonMWSObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Amazon Marketplace Web Service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AmazonMWSObject
    AmazonRedshiftTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Amazon Redshift table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AmazonRedshiftTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AmazonRedshiftTable
    AmazonRedshiftTableDatasetTypeProperties:
      description: Amazon Redshift table dataset properties.
      properties:
        schema:
          description: "The Amazon Redshift schema name. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The Amazon Redshift table name. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    AmazonS3Dataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: A single Amazon Simple Storage Service (S3) object or a set of S3
        objects.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AmazonS3DatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: AmazonS3Object
    AmazonS3DatasetTypeProperties:
      description: Amazon S3 dataset properties.
      properties:
        bucketName:
          description: "The name of the Amazon S3 bucket. Type: string (or Expression with
            resultType string)."
          type: object
        compression:
          $ref: "#/components/schemas/DatasetCompression"
        format:
          $ref: "#/components/schemas/DatasetStorageFormat"
        key:
          description: "The key of the Amazon S3 object. Type: string (or Expression with
            resultType string)."
          type: object
        modifiedDatetimeEnd:
          description: "The end of S3 object's modified datetime. Type: string (or
            Expression with resultType string)."
          type: object
        modifiedDatetimeStart:
          description: "The start of S3 object's modified datetime. Type: string (or
            Expression with resultType string)."
          type: object
        prefix:
          description: "The prefix filter for the S3 object name. Type: string (or
            Expression with resultType string)."
          type: object
        version:
          description: "The version for the S3 object. Type: string (or Expression with
            resultType string)."
          type: object
      required:
        - bucketName
    AmazonS3Location:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of amazon S3 dataset.
      properties:
        bucketName:
          description: "Specify the bucketName of amazon S3. Type: string (or Expression
            with resultType string)"
          type: object
        version:
          description: "Specify the version of amazon S3. Type: string (or Expression with
            resultType string)."
          type: object
      type: object
    AvroDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Avro dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AvroDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: Avro
    AvroDatasetTypeProperties:
      description: Avro dataset properties.
      properties:
        avroCompressionCodec:
          enum:
            - none
            - deflate
            - snappy
            - xz
            - bzip2
          type: string
          x-ms-enum:
            modelAsString: true
            name: avroCompressionCodec
        avroCompressionLevel:
          maximum: 9
          minimum: 1
          type: integer
        location:
          $ref: "#/components/schemas/DatasetLocation"
      required:
        - location
    AvroFormat:
      allOf:
        - $ref: "#/components/schemas/DatasetStorageFormat"
      description: The data stored in Avro format.
      type: object
    AzureBlobDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure Blob storage.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureBlobDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AzureBlob
    AzureBlobDatasetTypeProperties:
      description: Azure Blob dataset properties.
      properties:
        compression:
          $ref: "#/components/schemas/DatasetCompression"
        fileName:
          description: "The name of the Azure Blob. Type: string (or Expression with
            resultType string)."
          type: object
        folderPath:
          description: "The path of the Azure Blob storage. Type: string (or Expression
            with resultType string)."
          type: object
        format:
          $ref: "#/components/schemas/DatasetStorageFormat"
        modifiedDatetimeEnd:
          description: "The end of Azure Blob's modified datetime. Type: string (or
            Expression with resultType string)."
          type: object
        modifiedDatetimeStart:
          description: "The start of Azure Blob's modified datetime. Type: string (or
            Expression with resultType string)."
          type: object
        tableRootLocation:
          description: "The root of blob path. Type: string (or Expression with resultType
            string)."
          type: object
    AzureBlobFSDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure Data Lake Storage Gen2 storage.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureBlobFSDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AzureBlobFSFile
    AzureBlobFSDatasetTypeProperties:
      description: Azure Data Lake Storage Gen2 dataset properties.
      properties:
        compression:
          $ref: "#/components/schemas/DatasetCompression"
        fileName:
          description: "The name of the Azure Data Lake Storage Gen2. Type: string (or
            Expression with resultType string)."
          type: object
        folderPath:
          description: "The path of the Azure Data Lake Storage Gen2 storage. Type: string
            (or Expression with resultType string)."
          type: object
        format:
          $ref: "#/components/schemas/DatasetStorageFormat"
    AzureBlobFSLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of azure blobFS dataset.
      properties:
        fileSystem:
          description: "Specify the fileSystem of azure blobFS. Type: string (or Expression
            with resultType string)."
          type: object
      type: object
    AzureBlobStorageLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of azure blob dataset.
      properties:
        container:
          description: "Specify the container of azure blob. Type: string (or Expression
            with resultType string)."
          type: object
      type: object
    AzureDataExplorerDatasetTypeProperties:
      description: Azure Data Explorer (Kusto) dataset properties.
      properties:
        table:
          description: "The table name of the Azure Data Explorer database. Type: string
            (or Expression with resultType string)."
          type: object
    AzureDataExplorerTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure Data Explorer (Kusto) dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureDataExplorerDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: AzureDataExplorerTable
    AzureDataLakeStoreDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Azure Data Lake Store dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureDataLakeStoreDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AzureDataLakeStoreFile
    AzureDataLakeStoreDatasetTypeProperties:
      description: Azure Data Lake Store dataset properties.
      properties:
        compression:
          $ref: "#/components/schemas/DatasetCompression"
        fileName:
          description: "The name of the file in the Azure Data Lake Store. Type: string (or
            Expression with resultType string)."
          type: object
        folderPath:
          description: "Path to the folder in the Azure Data Lake Store. Type: string (or
            Expression with resultType string)."
          type: object
        format:
          $ref: "#/components/schemas/DatasetStorageFormat"
    AzureDataLakeStoreLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of azure data lake store dataset.
      type: object
    AzureFileStorageLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of file server dataset.
      type: object
    AzureMariaDBTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Azure Database for MariaDB dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AzureMariaDBTable
    AzureMySqlTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure MySQL database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureMySqlTableDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: AzureMySqlTable
    AzureMySqlTableDatasetTypeProperties:
      description: Azure MySQL database dataset properties.
      properties:
        table:
          description: "The name of Azure MySQL database table. Type: string (or Expression
            with resultType string)."
          type: object
        tableName:
          description: "The Azure MySQL database table name. Type: string (or Expression
            with resultType string)."
          type: object
    AzurePostgreSqlTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Azure PostgreSQL dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzurePostgreSqlTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AzurePostgreSqlTable
    AzurePostgreSqlTableDatasetTypeProperties:
      description: Azure PostgreSQL dataset properties.
      properties:
        schema:
          description: "The schema name of the Azure PostgreSQL database. Type: string (or
            Expression with resultType string)."
          type: object
        table:
          description: "The table name of the Azure PostgreSQL database. Type: string (or
            Expression with resultType string)."
          type: object
        tableName:
          description: "The table name of the Azure PostgreSQL database which includes both
            schema and table. Type: string (or Expression with resultType
            string)."
          type: object
    AzureSearchIndexDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure Search Index.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureSearchIndexDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: AzureSearchIndex
    AzureSearchIndexDatasetTypeProperties:
      description: Properties specific to this dataset type.
      properties:
        indexName:
          description: "The name of the Azure Search Index. Type: string (or Expression
            with resultType string)."
          type: object
      required:
        - indexName
    AzureSqlDWTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure SQL Data Warehouse dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureSqlDWTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AzureSqlDWTable
    AzureSqlDWTableDatasetTypeProperties:
      description: Azure SQL Data Warehouse dataset properties.
      properties:
        schema:
          description: "The schema name of the Azure SQL Data Warehouse. Type: string (or
            Expression with resultType string)."
          type: object
        table:
          description: "The table name of the Azure SQL Data Warehouse. Type: string (or
            Expression with resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    AzureSqlMITableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure SQL Managed Instance dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureSqlMITableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AzureSqlMITable
    AzureSqlMITableDatasetTypeProperties:
      description: Azure SQL Managed Instance dataset properties.
      properties:
        schema:
          description: "The schema name of the Azure SQL Managed Instance. Type: string (or
            Expression with resultType string)."
          type: object
        table:
          description: "The table name of the Azure SQL Managed Instance dataset. Type:
            string (or Expression with resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    AzureSqlTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure SQL Server database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureSqlTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: AzureSqlTable
    AzureSqlTableDatasetTypeProperties:
      description: Azure SQL dataset properties.
      properties:
        schema:
          description: "The schema name of the Azure SQL database. Type: string (or
            Expression with resultType string)."
          type: object
        table:
          description: "The table name of the Azure SQL database. Type: string (or
            Expression with resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    AzureTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Azure Table storage dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/AzureTableDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: AzureTable
    AzureTableDatasetTypeProperties:
      description: Azure Table dataset properties.
      properties:
        tableName:
          description: "The table name of the Azure Table storage. Type: string (or
            Expression with resultType string)."
          type: object
      required:
        - tableName
    BinaryDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Binary dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/BinaryDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: Binary
    BinaryDatasetTypeProperties:
      description: Binary dataset properties.
      properties:
        compression:
          $ref: "#/components/schemas/DatasetCompression"
        location:
          $ref: "#/components/schemas/DatasetLocation"
      required:
        - location
      type: object
    CassandraTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Cassandra database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/CassandraTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: CassandraTable
    CassandraTableDatasetTypeProperties:
      description: Cassandra dataset properties.
      properties:
        keyspace:
          description: "The keyspace of the Cassandra database. Type: string (or Expression
            with resultType string)."
          type: object
        tableName:
          description: "The table name of the Cassandra database. Type: string (or
            Expression with resultType string)."
          type: object
    CommonDataServiceForAppsEntityDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Common Data Service for Apps entity dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/CommonDataServiceForAppsEntityDatasetTypeProp\
            erties"
      type: object
      x-ms-discriminator-value: CommonDataServiceForAppsEntity
    CommonDataServiceForAppsEntityDatasetTypeProperties:
      description: Common Data Service for Apps entity dataset properties.
      properties:
        entityName:
          description: "The logical name of the entity. Type: string (or Expression with
            resultType string)."
          type: object
    CompressionLevel:
      description: All available compression levels.
      enum:
        - Optimal
        - Fastest
      title: Microsoft.Azure.Management.DataFactories.Models.CompressionLevel
      type: object
      x-ms-enum:
        modelAsString: true
        name: DatasetCompressionLevel
    ConcurObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Concur Service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: ConcurObject
    CosmosDbMongoDbApiCollectionDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The CosmosDB (MongoDB API) database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/CosmosDbMongoDbApiCollectionDatasetTypeProper\
            ties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: CosmosDbMongoDbApiCollection
    CosmosDbMongoDbApiCollectionDatasetTypeProperties:
      description: CosmosDB (MongoDB API) database dataset properties.
      properties:
        collection:
          description: "The collection name of the CosmosDB (MongoDB API) database. Type:
            string (or Expression with resultType string)."
          type: object
      required:
        - collection
    CosmosDbSqlApiCollectionDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Microsoft Azure CosmosDB (SQL API) Collection dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/CosmosDbSqlApiCollectionDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: CosmosDbSqlApiCollection
    CosmosDbSqlApiCollectionDatasetTypeProperties:
      description: CosmosDB (SQL API) Collection dataset properties.
      properties:
        collectionName:
          description: "CosmosDB (SQL API) collection name. Type: string (or Expression
            with resultType string)."
          type: object
      required:
        - collectionName
    CouchbaseTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Couchbase server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: CouchbaseTable
    CustomDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The custom dataset.
      properties:
        typeProperties:
          description: Custom dataset properties.
          type: object
          x-ms-client-flatten: true
      type: object
    Dataset:
      additionalProperties:
        type: object
      description: The Azure Data Factory nested object which identifies data within
        different data stores, such as tables, files, folders, and documents.
      discriminator:
        propertyName: type
      properties:
        annotations:
          description: List of tags that can be used for describing the Dataset.
          items:
            type: object
          type: array
        description:
          description: Dataset description.
          type: string
        folder:
          description: The folder that this Dataset is in. If not specified, Dataset will
            appear at the root level.
          properties:
            name:
              description: The name of the folder that this Dataset is in.
              type: string
          type: object
        linkedServiceName:
          description: Linked service reference type.
          properties:
            parameters:
              additionalProperties:
                type: object
              description: An object mapping parameter names to argument values.
              type: object
            referenceName:
              description: Reference LinkedService name.
              type: string
            type:
              description: Linked service reference type.
              enum:
                - LinkedServiceReference
              type: string
          required:
            - type
            - referenceName
        parameters:
          additionalProperties:
            description: Definition of a single parameter for an entity.
            properties:
              defaultValue:
                description: Default value of parameter.
                type: object
              type:
                description: Parameter type.
                enum:
                  - Object
                  - String
                  - Int
                  - Float
                  - Bool
                  - Array
                  - SecureString
                type: string
                x-ms-enum:
                  modelAsString: true
                  name: ParameterType
            required:
              - type
            type: object
          description: Definition of all parameters for an entity.
          type: object
        schema:
          description: "Columns that define the physical type schema of the dataset. Type:
            array (or Expression with resultType array), itemType:
            DatasetSchemaDataElement."
          type: object
        structure:
          description: "Columns that define the structure of the dataset. Type: array (or
            Expression with resultType array), itemType: DatasetDataElement."
          type: object
        type:
          description: Type of dataset.
          type: string
      required:
        - type
        - linkedServiceName
      type: object
    DatasetBZip2Compression:
      allOf:
        - $ref: "#/components/schemas/DatasetCompression"
      description: The BZip2 compression method used on a dataset.
      properties: {}
      type: object
      x-ms-discriminator-value: BZip2
    DatasetCompression:
      additionalProperties:
        type: object
      description: The compression method used on a dataset.
      discriminator:
        propertyName: type
      properties:
        type:
          description: Type of dataset compression.
          type: string
      required:
        - type
      type: object
    DatasetDataElement:
      description: Columns that define the structure of the dataset.
      properties:
        name:
          description: "Name of the column. Type: string (or Expression with resultType
            string)."
          type: object
        type:
          description: "Type of the column. Type: string (or Expression with resultType
            string)."
          type: object
      type: object
    DatasetDeflateCompression:
      allOf:
        - $ref: "#/components/schemas/DatasetCompression"
      description: The Deflate compression method used on a dataset.
      properties:
        level:
          $ref: "#/components/schemas/CompressionLevel"
      type: object
      x-ms-discriminator-value: Deflate
    DatasetGZipCompression:
      allOf:
        - $ref: "#/components/schemas/DatasetCompression"
      description: The GZip compression method used on a dataset.
      properties:
        level:
          $ref: "#/components/schemas/CompressionLevel"
      type: object
      x-ms-discriminator-value: GZip
    DatasetLocation:
      additionalProperties:
        type: object
      description: Dataset location.
      properties:
        fileName:
          description: "Specify the file name of dataset. Type: string (or Expression with
            resultType string)."
          type: object
        folderPath:
          description: "Specify the folder path of dataset. Type: string (or Expression
            with resultType string)"
          type: object
        type:
          description: Type of dataset storage location.
          type: string
      required:
        - type
      type: object
    DatasetSchemaDataElement:
      additionalProperties:
        type: object
      description: Columns that define the physical type schema of the dataset.
      properties:
        name:
          description: "Name of the schema column. Type: string (or Expression with
            resultType string)."
          type: object
        type:
          description: "Type of the schema column. Type: string (or Expression with
            resultType string)."
          type: object
      type: object
    DatasetStorageFormat:
      additionalProperties:
        type: object
      description: The format definition of a storage.
      discriminator:
        propertyName: type
      properties:
        deserializer:
          description: "Deserializer. Type: string (or Expression with resultType string)."
          type: object
        serializer:
          description: "Serializer. Type: string (or Expression with resultType string)."
          type: object
        type:
          description: Type of dataset storage format.
          type: string
      required:
        - type
      type: object
    DatasetZipDeflateCompression:
      allOf:
        - $ref: "#/components/schemas/DatasetCompression"
      description: The ZipDeflate compression method used on a dataset.
      properties:
        level:
          $ref: "#/components/schemas/CompressionLevel"
      type: object
      x-ms-discriminator-value: ZipDeflate
    Db2TableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Db2 table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/Db2TableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: Db2Table
    Db2TableDatasetTypeProperties:
      description: Db2 table dataset properties.
      properties:
        schema:
          description: "The Db2 schema name. Type: string (or Expression with resultType
            string)."
          type: object
        table:
          description: "The Db2 table name. Type: string (or Expression with resultType
            string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    DelimitedTextDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Delimited text dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/DelimitedTextDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: DelimitedText
    DelimitedTextDatasetTypeProperties:
      description: DelimitedText dataset properties.
      properties:
        columnDelimiter:
          description: "The column delimiter. Type: string (or Expression with resultType
            string)."
          type: object
        compressionCodec:
          enum:
            - bzip2
            - gzip
            - deflate
            - zipDeflate
            - snappy
            - lz4
          type: object
          x-ms-enum:
            modelAsString: true
            name: compressionCodec
        compressionLevel:
          $ref: "#/components/schemas/CompressionLevel"
        encodingName:
          description: "The code page name of the preferred encoding. If miss, the default
            value is UTF-8, unless BOM denotes another Unicode encoding. Refer
            to the name column of the table in the following link to set
            supported values:
            https://msdn.microsoft.com/library/system.text.encoding.aspx. Type:
            string (or Expression with resultType string)."
          type: object
        escapeChar:
          description: "The escape character. Type: string (or Expression with resultType
            string)."
          type: object
        firstRowAsHeader:
          description: "When used as input, treat the first row of data as headers. When
            used as output,write the headers into the output as the first row of
            data. The default value is false. Type: boolean (or Expression with
            resultType boolean)."
          type: object
        location:
          $ref: "#/components/schemas/DatasetLocation"
        nullValue:
          description: "The null value string. Type: string (or Expression with resultType
            string)."
          type: object
        quoteChar:
          description: "The quote character. Type: string (or Expression with resultType
            string)."
          type: object
        rowDelimiter:
          description: "The row delimiter. Type: string (or Expression with resultType
            string)."
          type: object
      required:
        - location
    DocumentDbCollectionDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Microsoft Azure Document Database Collection dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/DocumentDbCollectionDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: DocumentDbCollection
    DocumentDbCollectionDatasetTypeProperties:
      description: DocumentDB Collection dataset properties.
      properties:
        collectionName:
          description: "Document Database collection name. Type: string (or Expression with
            resultType string)."
          type: object
      required:
        - collectionName
    DrillDatasetTypeProperties:
      description: Drill Dataset Properties
      properties:
        schema:
          description: "The schema name of the Drill. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of the Drill. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    DrillTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Drill server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/DrillDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: DrillTable
    DynamicsAXResourceDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The path of the Dynamics AX OData entity.
      properties:
        typeProperties:
          $ref: "#/components/schemas/DynamicsAXResourceDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: DynamicsAXResource
    DynamicsAXResourceDatasetTypeProperties:
      description: Dynamics AX OData resource dataset properties.
      properties:
        path:
          description: "The path of the Dynamics AX OData entity. Type: string (or
            Expression with resultType string)."
          type: object
      required:
        - path
    DynamicsCrmEntityDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Dynamics CRM entity dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/DynamicsCrmEntityDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: DynamicsCrmEntity
    DynamicsCrmEntityDatasetTypeProperties:
      description: Dynamics CRM entity dataset properties.
      properties:
        entityName:
          description: "The logical name of the entity. Type: string (or Expression with
            resultType string)."
          type: object
    DynamicsEntityDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Dynamics entity dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/DynamicsEntityDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: DynamicsEntity
    DynamicsEntityDatasetTypeProperties:
      description: Dynamics entity dataset properties.
      properties:
        entityName:
          description: "The logical name of the entity. Type: string (or Expression with
            resultType string)."
          type: object
    EloquaObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Eloqua server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: EloquaObject
    FileServerLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of file server dataset.
      type: object
    FileShareDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: An on-premises file system dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/FileShareDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: FileShare
    FileShareDatasetTypeProperties:
      description: On-premises file system dataset properties.
      properties:
        compression:
          $ref: "#/components/schemas/DatasetCompression"
        fileFilter:
          description: "Specify a filter to be used to select a subset of files in the
            folderPath rather than all files. Type: string (or Expression with
            resultType string)."
          type: object
        fileName:
          description: "The name of the on-premises file system. Type: string (or
            Expression with resultType string)."
          type: object
        folderPath:
          description: "The path of the on-premises file system. Type: string (or
            Expression with resultType string)."
          type: object
        format:
          $ref: "#/components/schemas/DatasetStorageFormat"
        modifiedDatetimeEnd:
          description: "The end of file's modified datetime. Type: string (or Expression
            with resultType string)."
          type: object
        modifiedDatetimeStart:
          description: "The start of file's modified datetime. Type: string (or Expression
            with resultType string)."
          type: object
    FtpServerLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of ftp server dataset.
      type: object
    GenericDatasetTypeProperties:
      description: Properties specific to this dataset type.
      properties:
        tableName:
          description: "The table name. Type: string (or Expression with resultType
            string)."
          type: object
    GoogleAdWordsObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Google AdWords service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: GoogleAdWordsObject
    GoogleBigQueryDatasetTypeProperties:
      description: Google BigQuery Dataset Properties
      properties:
        dataset:
          description: "The database name of the Google BigQuery. Type: string (or
            Expression with resultType string)."
          type: object
        table:
          description: "The table name of the Google BigQuery. Type: string (or Expression
            with resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using database +
            table properties instead.
          type: object
    GoogleBigQueryObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Google BigQuery service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GoogleBigQueryDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: GoogleBigQueryObject
    GoogleCloudStorageLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of Google Cloud Storage dataset.
      properties:
        bucketName:
          description: "Specify the bucketName of Google Cloud Storage. Type: string (or
            Expression with resultType string)"
          type: object
        version:
          description: "Specify the version of Google Cloud Storage. Type: string (or
            Expression with resultType string)."
          type: object
      type: object
    GreenplumDatasetTypeProperties:
      description: Greenplum Dataset Properties
      properties:
        schema:
          description: "The schema name of Greenplum. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of Greenplum. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    GreenplumTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Greenplum Database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GreenplumDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: GreenplumTable
    HBaseObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: HBase server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: HBaseObject
    HdfsLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of HDFS.
      type: object
    HiveDatasetTypeProperties:
      description: Hive Properties
      properties:
        schema:
          description: "The schema name of the Hive. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of the Hive. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    HiveObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Hive Server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/HiveDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: HiveObject
    HttpDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: A file in an HTTP web server.
      properties:
        typeProperties:
          $ref: "#/components/schemas/HttpDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: HttpFile
    HttpDatasetTypeProperties:
      description: Properties specific to this dataset type.
      properties:
        additionalHeaders:
          description: "The headers for the HTTP Request. e.g.
            request-header-name-1:request-header-value-1\r

            ...\r

            request-header-name-n:request-header-value-n Type:
            string (or Expression with resultType string)."
          type: object
        compression:
          $ref: "#/components/schemas/DatasetCompression"
        format:
          $ref: "#/components/schemas/DatasetStorageFormat"
        relativeUrl:
          description: "The relative URL based on the URL in the HttpLinkedService refers
            to an HTTP file Type: string (or Expression with resultType
            string)."
          type: object
        requestBody:
          description: "The body for the HTTP request. Type: string (or Expression with
            resultType string)."
          type: object
        requestMethod:
          description: "The HTTP method for the HTTP request. Type: string (or Expression
            with resultType string)."
          type: object
    HttpServerLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of http server.
      properties:
        relativeUrl:
          description: "Specify the relativeUrl of http server. Type: string (or Expression
            with resultType string)"
          type: object
      type: object
    HubspotObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Hubspot Service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: HubspotObject
    ImpalaDatasetTypeProperties:
      description: Impala Dataset Properties
      properties:
        schema:
          description: "The schema name of the Impala. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of the Impala. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    ImpalaObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Impala server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/ImpalaDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: ImpalaObject
    InformixTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Informix table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/InformixTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: InformixTable
    InformixTableDatasetTypeProperties:
      description: Informix table dataset properties.
      properties:
        tableName:
          description: "The Informix table name. Type: string (or Expression with
            resultType string)."
          type: object
    JiraObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Jira Service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: JiraObject
    JsonDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Json dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/JsonDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: Json
    JsonDatasetTypeProperties:
      description: Json dataset properties.
      properties:
        compression:
          $ref: "#/components/schemas/DatasetCompression"
        encodingName:
          description: "The code page name of the preferred encoding. If not specified, the
            default value is UTF-8, unless BOM denotes another Unicode encoding.
            Refer to the name column of the table in the following link to set
            supported values:
            https://msdn.microsoft.com/library/system.text.encoding.aspx. Type:
            string (or Expression with resultType string)."
          type: object
        location:
          $ref: "#/components/schemas/DatasetLocation"
      required:
        - location
    JsonFormat:
      allOf:
        - $ref: "#/components/schemas/DatasetStorageFormat"
      description: The data stored in JSON format.
      properties:
        encodingName:
          description: "The code page name of the preferred encoding. If not provided, the
            default value is 'utf-8', unless the byte order mark (BOM) denotes
            another Unicode encoding. The full list of supported values can be
            found in the 'Name' column of the table of encodings in the
            following reference: https://go.microsoft.com/fwlink/?linkid=861078.
            Type: string (or Expression with resultType string)."
          type: object
        filePattern:
          $ref: "#/components/schemas/JsonFormatFilePattern"
        jsonNodeReference:
          description: 'The JSONPath of the JSON array element to be flattened. Example:
            "$.ArrayPath". Type: string (or Expression with resultType string).'
          type: object
        jsonPathDefinition:
          description: 'The JSONPath definition for each column mapping with a customized
            column name to extract data from JSON file. For fields under root
            object, start with "$"; for fields inside the array chosen by
            jsonNodeReference property, start from the array element. Example:
            {"Column1": "$.Column1Path", "Column2": "Column2PathInArray"}. Type:
            object (or Expression with resultType object).'
          type: object
        nestingSeparator:
          description: "The character used to separate nesting levels. Default value is '.'
            (dot). Type: string (or Expression with resultType string)."
          type: object
      type: object
    JsonFormatFilePattern:
      description: JSON format file pattern. A property of JsonFormat.
      enum:
        - setOfObjects
        - arrayOfObjects
      type: object
      x-ms-enum:
        modelAsString: true
        name: JsonFormatFilePattern
    MagentoObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Magento server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: MagentoObject
    MariaDBTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: MariaDB server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: MariaDBTable
    MarketoObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Marketo server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: MarketoObject
    MicrosoftAccessTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Microsoft Access table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/MicrosoftAccessTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: MicrosoftAccessTable
    MicrosoftAccessTableDatasetTypeProperties:
      description: Microsoft Access table dataset properties.
      properties:
        tableName:
          description: "The Microsoft Access table name. Type: string (or Expression with
            resultType string)."
          type: object
    MongoDbCollectionDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The MongoDB database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/MongoDbCollectionDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: MongoDbCollection
    MongoDbCollectionDatasetTypeProperties:
      description: MongoDB database dataset properties.
      properties:
        collectionName:
          description: "The table name of the MongoDB database. Type: string (or Expression
            with resultType string)."
          type: object
      required:
        - collectionName
    MongoDbV2CollectionDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The MongoDB database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/MongoDbV2CollectionDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: MongoDbV2Collection
    MongoDbV2CollectionDatasetTypeProperties:
      description: MongoDB database dataset properties.
      properties:
        collection:
          description: "The collection name of the MongoDB database. Type: string (or
            Expression with resultType string)."
          type: object
      required:
        - collection
    MySqlTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The MySQL table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/MySqlTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: MySqlTable
    MySqlTableDatasetTypeProperties:
      description: MySql table dataset properties.
      properties:
        tableName:
          description: "The MySQL table name. Type: string (or Expression with resultType
            string)."
          type: object
    NetezzaTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Netezza dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/NetezzaTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: NetezzaTable
    NetezzaTableDatasetTypeProperties:
      description: Netezza dataset properties.
      properties:
        schema:
          description: "The schema name of the Netezza. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of the Netezza. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    ODataResourceDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Open Data Protocol (OData) resource dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/ODataResourceDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: ODataResource
    ODataResourceDatasetTypeProperties:
      description: OData dataset properties.
      properties:
        path:
          description: "The OData resource path. Type: string (or Expression with
            resultType string)."
          type: object
    OdbcTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The ODBC table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/OdbcTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: OdbcTable
    OdbcTableDatasetTypeProperties:
      description: ODBC table dataset properties.
      properties:
        tableName:
          description: "The ODBC table name. Type: string (or Expression with resultType
            string)."
          type: object
    Office365Dataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Office365 account.
      properties:
        typeProperties:
          $ref: "#/components/schemas/Office365DatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: Office365Table
    Office365DatasetTypeProperties:
      description: Office365 dataset properties.
      properties:
        predicate:
          description: "A predicate expression that can be used to filter the specific rows
            to extract from Office 365. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: "Name of the dataset to extract from Office 365. Type: string (or
            Expression with resultType string)."
          type: object
      required:
        - tableName
    OracleServiceCloudObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Oracle Service Cloud dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: OracleServiceCloudObject
    OracleTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The on-premises Oracle database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/OracleTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: OracleTable
    OracleTableDatasetTypeProperties:
      description: On-premises Oracle dataset properties.
      properties:
        schema:
          description: "The schema name of the on-premises Oracle database. Type: string
            (or Expression with resultType string)."
          type: object
        table:
          description: "The table name of the on-premises Oracle database. Type: string (or
            Expression with resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    OrcDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: ORC dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/OrcDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: Orc
    OrcDatasetTypeProperties:
      description: ORC dataset properties.
      properties:
        location:
          $ref: "#/components/schemas/DatasetLocation"
      required:
        - location
    OrcFormat:
      allOf:
        - $ref: "#/components/schemas/DatasetStorageFormat"
      description: The data stored in Optimized Row Columnar (ORC) format.
      type: object
    ParquetDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Parquet dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/ParquetDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: Parquet
    ParquetDatasetTypeProperties:
      description: Parquet dataset properties.
      properties:
        compressionCodec:
          enum:
            - none
            - gzip
            - snappy
            - lzo
          type: object
          x-ms-enum:
            modelAsString: true
            name: compressionCodec
        location:
          $ref: "#/components/schemas/DatasetLocation"
      required:
        - location
    ParquetFormat:
      allOf:
        - $ref: "#/components/schemas/DatasetStorageFormat"
      description: The data stored in Parquet format.
      type: object
    PaypalObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Paypal Service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: PaypalObject
    PhoenixDatasetTypeProperties:
      description: Phoenix Dataset Properties
      properties:
        schema:
          description: "The schema name of the Phoenix. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of the Phoenix. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    PhoenixObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Phoenix server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/PhoenixDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: PhoenixObject
    PostgreSqlTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The PostgreSQL table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/PostgreSqlTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: PostgreSqlTable
    PostgreSqlTableDatasetTypeProperties:
      description: PostgreSQL table dataset properties.
      properties:
        schema:
          description: "The PostgreSQL schema name. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The PostgreSQL table name. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    PrestoDatasetTypeProperties:
      description: Presto Dataset Properties
      properties:
        schema:
          description: "The schema name of the Presto. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of the Presto. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    PrestoObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Presto server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/PrestoDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: PrestoObject
    QuickBooksObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: QuickBooks server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: QuickBooksObject
    RelationalTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The relational table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/RelationalTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: RelationalTable
    RelationalTableDatasetTypeProperties:
      description: Relational table dataset properties.
      properties:
        tableName:
          description: "The relational table name. Type: string (or Expression with
            resultType string)."
          type: object
    ResponsysObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Responsys dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: ResponsysObject
    RestResourceDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: A Rest service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/RestResourceDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: RestResource
    RestResourceDatasetTypeProperties:
      description: Properties specific to this dataset type.
      properties:
        additionalHeaders:
          description: "The additional HTTP headers in the request to the RESTful API.
            Type: string (or Expression with resultType string)."
          type: object
        paginationRules:
          description: "The pagination rules to compose next page requests. Type: string
            (or Expression with resultType string)."
          type: object
        relativeUrl:
          description: "The relative URL to the resource that the RESTful API provides.
            Type: string (or Expression with resultType string)."
          type: object
        requestBody:
          description: "The HTTP request body to the RESTful API if requestMethod is POST.
            Type: string (or Expression with resultType string)."
          type: object
        requestMethod:
          description: "The HTTP method used to call the RESTful API. The default is GET.
            Type: string (or Expression with resultType string)."
          type: object
    SalesforceMarketingCloudObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Salesforce Marketing Cloud dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: SalesforceMarketingCloudObject
    SalesforceObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Salesforce object dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SalesforceObjectDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: SalesforceObject
    SalesforceObjectDatasetTypeProperties:
      description: Salesforce object dataset properties.
      properties:
        objectApiName:
          description: "The Salesforce object API name. Type: string (or Expression with
            resultType string)."
          type: object
    SalesforceServiceCloudObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Salesforce Service Cloud object dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SalesforceServiceCloudObjectDatasetTypeProper\
            ties"
      type: object
      x-ms-discriminator-value: SalesforceServiceCloudObject
    SalesforceServiceCloudObjectDatasetTypeProperties:
      description: Salesforce Service Cloud object dataset properties.
      properties:
        objectApiName:
          description: "The Salesforce Service Cloud object API name. Type: string (or
            Expression with resultType string)."
          type: object
    SapBwCubeDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The SAP BW cube dataset.
      properties: {}
      type: object
      x-ms-discriminator-value: SapBwCube
    SapCloudForCustomerResourceDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The path of the SAP Cloud for Customer OData entity.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SapCloudForCustomerResourceDatasetTypePropert\
            ies"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: SapCloudForCustomerResource
    SapCloudForCustomerResourceDatasetTypeProperties:
      description: Sap Cloud For Customer OData resource dataset properties.
      properties:
        path:
          description: "The path of the SAP Cloud for Customer OData entity. Type: string
            (or Expression with resultType string)."
          type: object
      required:
        - path
    SapEccResourceDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The path of the SAP ECC OData entity.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SapEccResourceDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: SapEccResource
    SapEccResourceDatasetTypeProperties:
      description: Sap ECC OData resource dataset properties.
      properties:
        path:
          description: "The path of the SAP ECC OData entity. Type: string (or Expression
            with resultType string)."
          type: object
      required:
        - path
    SapHanaTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: SAP HANA Table properties.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SapHanaTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: SapHanaTable
    SapHanaTableDatasetTypeProperties:
      description: SAP HANA Table properties.
      properties:
        schema:
          description: "The schema name of SAP HANA. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of SAP HANA. Type: string (or Expression with
            resultType string)."
          type: object
    SapOpenHubTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Sap Business Warehouse Open Hub Destination Table properties.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SapOpenHubTableDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: SapOpenHubTable
    SapOpenHubTableDatasetTypeProperties:
      description: Sap Business Warehouse Open Hub Destination Table properties.
      properties:
        baseRequestId:
          description: "The ID of request for delta loading. Once it is set, only data with
            requestId larger than the value of this property will be retrieved.
            The default value is 0. Type: integer (or Expression with resultType
            integer )."
          type: object
        excludeLastRequest:
          description: "Whether to exclude the records of the last request. The default
            value is true. Type: boolean (or Expression with resultType
            boolean)."
          type: object
        openHubDestinationName:
          description: "The name of the Open Hub Destination with destination type as
            Database Table. Type: string (or Expression with resultType
            string)."
          type: object
      required:
        - openHubDestinationName
    SapTableResourceDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: SAP Table Resource properties.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SapTableResourceDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: SapTableResource
    SapTableResourceDatasetTypeProperties:
      description: SAP Table Resource properties.
      properties:
        tableName:
          description: "The name of the SAP Table. Type: string (or Expression with
            resultType string)."
          type: object
      required:
        - tableName
    ServiceNowObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: ServiceNow server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: ServiceNowObject
    SftpLocation:
      allOf:
        - $ref: "#/components/schemas/DatasetLocation"
      description: The location of SFTP dataset.
      type: object
    ShopifyObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Shopify Service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: ShopifyObject
    SparkDatasetTypeProperties:
      description: Spark Properties
      properties:
        schema:
          description: "The schema name of the Spark. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of the Spark. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    SparkObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Spark Server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SparkDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: SparkObject
    SqlServerTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The on-premises SQL Server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SqlServerTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: SqlServerTable
    SqlServerTableDatasetTypeProperties:
      description: On-premises SQL Server dataset properties.
      properties:
        schema:
          description: "The schema name of the SQL Server dataset. Type: string (or
            Expression with resultType string)."
          type: object
        table:
          description: "The table name of the SQL Server dataset. Type: string (or
            Expression with resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    SquareObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Square Service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: SquareObject
    SybaseTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Sybase table dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/SybaseTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: SybaseTable
    SybaseTableDatasetTypeProperties:
      description: Sybase table dataset properties.
      properties:
        tableName:
          description: "The Sybase table name. Type: string (or Expression with resultType
            string)."
          type: object
    TeradataTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The Teradata database dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/TeradataTableDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: TeradataTable
    TeradataTableDatasetTypeProperties:
      description: Teradata dataset properties.
      properties:
        database:
          description: "The database name of Teradata. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of Teradata. Type: string (or Expression with
            resultType string)."
          type: object
    TextFormat:
      allOf:
        - $ref: "#/components/schemas/DatasetStorageFormat"
      description: The data stored in text format.
      properties:
        columnDelimiter:
          description: "The column delimiter. Type: string (or Expression with resultType
            string)."
          type: object
        encodingName:
          description: "The code page name of the preferred encoding. If miss, the default
            value is ΓÇ£utf-8ΓÇ¥, unless BOM denotes another Unicode encoding.
            Refer to the ΓÇ£NameΓÇ¥ column of the table in the following link to
            set supported values:
            https://msdn.microsoft.com/library/system.text.encoding.aspx. Type:
            string (or Expression with resultType string)."
          type: object
        escapeChar:
          description: "The escape character. Type: string (or Expression with resultType
            string)."
          type: object
        firstRowAsHeader:
          description: "When used as input, treat the first row of data as headers. When
            used as output,write the headers into the output as the first row of
            data. The default value is false. Type: boolean (or Expression with
            resultType boolean)."
          type: object
        nullValue:
          description: "The null value string. Type: string (or Expression with resultType
            string)."
          type: object
        quoteChar:
          description: "The quote character. Type: string (or Expression with resultType
            string)."
          type: object
        rowDelimiter:
          description: "The row delimiter. Type: string (or Expression with resultType
            string)."
          type: object
        skipLineCount:
          description: "The number of lines/rows to be skipped when parsing text files. The
            default value is 0. Type: integer (or Expression with resultType
            integer)."
          type: object
        treatEmptyAsNull:
          description: "Treat empty column values in the text file as null. The default
            value is true. Type: boolean (or Expression with resultType
            boolean)."
          type: object
      type: object
    VerticaDatasetTypeProperties:
      description: Vertica Properties
      properties:
        schema:
          description: "The schema name of the Vertica. Type: string (or Expression with
            resultType string)."
          type: object
        table:
          description: "The table name of the Vertica. Type: string (or Expression with
            resultType string)."
          type: object
        tableName:
          description: This property will be retired. Please consider using schema + table
            properties instead.
          type: object
    VerticaTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Vertica dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/VerticaDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: VerticaTable
    WebTableDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: The dataset points to a HTML table in the web page.
      properties:
        typeProperties:
          $ref: "#/components/schemas/WebTableDatasetTypeProperties"
      required:
        - typeProperties
      type: object
      x-ms-discriminator-value: WebTable
    WebTableDatasetTypeProperties:
      description: Web table dataset properties.
      properties:
        index:
          description: "The zero-based index of the table in the web page. Type: integer
            (or Expression with resultType integer), minimum: 0."
          type: object
        path:
          description: "The relative URL to the web page from the linked service URL. Type:
            string (or Expression with resultType string)."
          type: object
      required:
        - index
    XeroObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Xero Service dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: XeroObject
    ZohoObjectDataset:
      allOf:
        - $ref: "#/components/schemas/Dataset"
      description: Zoho server dataset.
      properties:
        typeProperties:
          $ref: "#/components/schemas/GenericDatasetTypeProperties"
      type: object
      x-ms-discriminator-value: ZohoObject
